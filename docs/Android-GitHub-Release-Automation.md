# Android GitHub 发布自动化（已迁移）

Android APK 发布已合并到统一客户端工作流：`.github/workflows/release-clients.yml`。

请使用 `docs/Client-GitHub-Release-Automation.md` 的最新说明。该工作流仍会维护 `android-latest/latest.json`，因此 Android 应用的更新检查行为不变。
